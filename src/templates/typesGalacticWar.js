import PropTypes from 'prop-types';

var propTypesTemplate = {
   model: PropTypes.object.isRequired,
   buttonCallback: PropTypes.func.isRequired,
   shopCallback: PropTypes.func.isRequired
}

export {propTypesTemplate}
